apiVersion: v1
kind: Service

metadata:
  name: renuja-devops-app-service
  namespace: renuja-devops

spec:
  selector:
    app: renuja-devops-app

  ports:
    - port: 80
      targetPort: 5000

  type: ClusterIP
